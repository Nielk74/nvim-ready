#pragma once

namespace MyApp
{
    /// Simple math utilities — no platform headers needed.
    inline int add(int a, int b)      { return a + b; }
    inline int subtract(int a, int b) { return a - b; }
    inline int multiply(int a, int b) { return a * b; }
}
